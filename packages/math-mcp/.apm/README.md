# Math MCP Server

A trivial MCP (Model Context Protocol) server that provides basic math operations.

## Setup

Install dependencies:
```bash
npm install
```

## Running

Start the server:
```bash
npm start
```

## Tools

- `add(a: number, b: number)` - Adds two numbers and returns the result
